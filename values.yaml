postgresql:
  enabled: true
  image:
    repository: postgres
    tag: 9.6.14
  persistence:
    enabled: true
    mountPath: /data
  # dataDir cannot be the mountPath,
  # it has to be a subdirectory
  postgresqlDataDir: /data/pgdata
  postgresqlDatabase: storyscript
  postgresqlUsername: storyscript
  postgresqlPassword: storyscript

nginx-ingress:
 rbac:
  create: true
 controller:
  service:
   loadBalancerIP:

hello-world:
  enabled: false
  image: storyscript/hello-world

database:
  image: storyscript/database

gateway:
  image: asyncy/http:v0.2.1-33-g536a1cb
  dns:
    subdomain: local
    domain: storyscript.test

graphql:
  image: asyncy/platform-graphql
  postgresqlUsername: asyncy_authenticator
  postgresqlPassword: PLEASE_CHANGE_ME

runtime:
  image: asyncy/platform-engine-prod:v0.3.1-755-ga9826ad

synapse:
  image: storyscript/synapse

auth:
 jwt:
  key:
 github:
  client:
   id:
   secret:
