apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-database-credentials"
type: Opaque
data:
  sqitch-url: {{ include "storyscript.databaseUrl" (merge (dict "sqitch" true "superuser" true) .) | b64enc }}
  superuser-url: {{ include "storyscript.databaseUrl" (merge (dict "superuser" true) .) | b64enc }}
  superuser-url-search-path: {{ include "storyscript.databaseUrl" (merge (dict "superuser" true "search_path" true) .) | b64enc }}
  public-url: {{ include "storyscript.databaseUrl" . | b64enc }}
