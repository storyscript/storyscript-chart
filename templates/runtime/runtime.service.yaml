apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-runtime"
  labels:
    app: runtime
spec:
  selector:
    app: runtime
  ports:
    - port: 8084
      name: story-endpoint
      protocol: TCP
      targetPort: 8084
