apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "{{ .Release.Name }}-graphql"
  labels:
    app: graphql
spec:
  rules:
    - http:
        paths:
        - path: /graphql
          backend:
            serviceName: "{{ .Release.Name }}-graphql"
            servicePort: 5000
